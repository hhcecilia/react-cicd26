pipeline{
    agent any
    stages{
        stage('build'){
            agent{
                docker{
                    image 'node:22.19.0-alpine'
                    reuseNode true
                }
            }
            steps{
                sh '''
                    ls -la
                    node --version
                    npm --version
                '''
            }
        }
    }
}
