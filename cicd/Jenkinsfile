pipeline {
    agent any
    stages {
        //stage('Build') {
        //    agent {
        //        docker {
        //            image 'node:22.19.0-alpine'
        //            reuseNode true
        //            args '-v jenkins-data:/var/jenkins_home'
        //        }
        //    }
        //    steps {
        //        sh '''
        //            ls -la
        //            node --version
        //            npm --version
        //            npm install
        //            npm run build
        //            ls -la
        //        '''
        //    }
        //}
        //stage('Test') {
        //    agent {
        //        docker {
        //            image 'node:22.19.0-alpine'
        //            reuseNode true
        //            args '-v jenkins-data:/var/jenkins_home'
        //        }
        //    }
        //    steps {
        //        sh '''
        //            test -f build/index.html
        //            CI=true npm test
        //        '''
        //    }
        //}
        stage('AWS') {
            agent {
                docker {
                    image 'amazon/aws-cli:2.34.14'
                    reuseNode true
                    args '--entrypoint=""'
                }
            }
            steps {
                withCredentials([usernamePassword(credentialsId: 'reactcicdtemp', passwordVariable: 'WS_SECRET_ACCESS_KEY', usernameVariable: 'WS_ACCESS_KEY_ID')]) {
                // this block will have access to the AWS credentials as environment variables WS_ACCESS_KEY_ID and WS_SECRET_ACCESS_KEY
}
                    sh '''
                        aws --version
                        aws s3 ls
                        echo "Hello, s3" > index.html
                        # aws s3 cp index.html s3://reactcicd26/index.html
                        aws s3 sync build s3://reactcicd26
                    '''
                }
            }
        }
    }
